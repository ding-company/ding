spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  main:
    allow-bean-definition-overriding: true
    banner-mode: "off"

  datasource:
    url: jdbc:h2:mem:///test_customer
jwt:
  secret: "very-very-long-secret-key-at-least-256bit"
  otp-token-validity: 5m
  access-token-validity: 30m
  pre-auth-token-validity: 30m