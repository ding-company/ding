package `in`.ding.point.domain.model.enumerate

enum class PointTransactionType {
    EARN,
    USE,
    EXPIRE,
    ADJUST,
    CANCEL,
    REVERSE_USE,
    REVERSE_EARN,
    HOLD,
    RELEASE_HOLD
}
