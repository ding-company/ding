package `in`.ding.point.domain.entity.enumerate

enum class PointTransactionType {
    EARN,
    USE,
    EXPIRE,
    ADJUST,
    CANCEL,
    REVERSE_USE,
    REVERSE_EARN,
    HOLD,
    RELEASE_HOLD
}
